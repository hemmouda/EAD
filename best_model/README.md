# The best model

This is the model whose results were presented.

The included [config folder](config_used/) contains the config files used during the preprocessing and training. You can learn more about how to use them in the [code part](/code).


The mappings between the new names and the original TUAR code names can be found [here](/code/molding/name_mappings.csv).